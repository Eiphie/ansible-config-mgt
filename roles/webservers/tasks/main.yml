#SPDX-License-Identifier: MIT-0
---
# tasks file for webserver
- name: install apache
  become: true
  remote_user: ubuntu
  ansible.builtin.yum:
    name: "httpd"
    state: present

- name: install git
  become: true
  remote_user: ubuntu
  ansible.builtin.yum:
    name: "git"
    state: present

- name: clone a repo
  become: true
  remote_user: ubuntu
  ansible.builtin.git:
    repo: https://github.com/Eiphie/tooling.git
    dest: /var/www/html
    force: yes

- name: copy html content to one level up
  become: true
  remote_user: ubuntu
  command: cp -r /var/www/html/html/ /var/www/

- name: Start service httpd, if not started
  become: true
  remote_user: ubuntu
  ansible.builtin.service:
    name: httpd
    state: started

- name: recursively remove /var/www/html/html/ directory
  become: true
  remote_user: ubuntu
  ansible.builtin.file:
    path: /var/www/html/html
    state: absent
